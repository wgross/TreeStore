<UserControl x:Class="Kosmograph.Desktop.Controls.ShowRelationshipsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Kosmograph.Desktop.Controls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="relationshipsListBoxStyle" TargetType="{x:Type ListBox}">
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" />
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderThickness" Value="0" />
        </Style>
    </UserControl.Resources>
    <DockPanel
        LastChildFill="True"
        Margin="0,0,0,0">
        <TextBlock DockPanel.Dock="Top"
            Text="Relationships"
            Foreground="DarkGray"
            FontFamily="Bold" />
        <ListBox
            Name="tagListBox"
            Panel.ZIndex="0"
            Style="{StaticResource relationshipsListBoxStyle}"
            ItemsSource="{Binding Relationships}"
            SelectedItem="{Binding SelectedRelationship, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            MouseMove="relationshipsListBox_MouseMove">
            <!--<ListBox.InputBindings>
            MouseDoubleClick="relationshipListBox_MouseDoubleClick"
                <KeyBinding Key="Insert"
                                        Command="{Binding CreateTagCommand}" />
                <KeyBinding Key="Return"
                                        Command="{Binding EditTagCommand}"
                                        CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource FindAncestor, AncestorType=ListBox}}" />
                <KeyBinding Key="Delete"
                                Command="{Binding DeleteTagCommand}"
                                CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource FindAncestor, AncestorType=ListBox}}" />
            </ListBox.InputBindings>-->
        </ListBox>
    </DockPanel>
</UserControl>